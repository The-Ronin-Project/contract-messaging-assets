{
  "$id": "asset-command-v1",
  "title": "Asset Commands",
  "description": "Commands for managing Assets.",
  "type": "object",
  "properties": {
    "object": {
      "$ref": "#/$defs/Asset"
    }
  },
  "$defs":  {
    "ResourceType": {
      "type": "string",
      "enum": [
        "DiagnosticReport",
        "DocumentReference",
        "Patient"
      ]
    },
    "Asset": {
      "type": "object",
      "description": "A Patient Asset",
      "properties": {
        "action": {
          "type": "string",
          "description": "The action of the command. CREATE, UPDATE, READ, DELETE"
        },
        "tenantId": {
          "type": "string"
        },
        "patientId": {
          "type": "string"
        },
        "url": {
          "type": "string"
        },
        "resourceType": {
          "$ref": "#/$defs/ResourceType"
        },
        "resourceId": {
          "type": "string"
        },
        "resourceAttribute": {
          "type": "string"
        }
      },
      "required": [
        "tenantId",
        "patientId",
        "url",
        "resourceType",
        "resourceAttribute"
      ]
    }
  }
}
